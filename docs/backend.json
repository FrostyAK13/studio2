{
  "entities": {
    "Stock": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Stock",
      "type": "object",
      "description": "Represents a financial stock whose data is being analyzed by the EMPORER application.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the Stock entity."
        },
        "symbol": {
          "type": "string",
          "description": "The unique ticker symbol of the stock (e.g., AAPL, MSFT)."
        },
        "companyName": {
          "type": "string",
          "description": "The full name of the company associated with the stock symbol."
        },
        "exchange": {
          "type": "string",
          "description": "The stock exchange where the stock is primarily traded (e.g., NASDAQ, NYSE)."
        },
        "lastHistoricalPriceFetchDate": {
          "type": "string",
          "description": "The date and time when the historical price data for this stock was last successfully fetched from a third-party API.",
          "format": "date-time"
        }
      },
      "required": [
        "id",
        "symbol",
        "companyName",
        "exchange"
      ]
    },
    "HistoricalPrice": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "HistoricalPrice",
      "type": "object",
      "description": "Stores a single day's historical price and volume data for a specific stock.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the HistoricalPrice entity."
        },
        "stockId": {
          "type": "string",
          "description": "Reference to the Stock entity this historical price data belongs to. (Relationship: Stock 1:N HistoricalPrice)"
        },
        "date": {
          "type": "string",
          "description": "The calendar date for which this historical price data is recorded.",
          "format": "date"
        },
        "open": {
          "type": "number",
          "description": "The opening price of the stock on the specified date."
        },
        "high": {
          "type": "number",
          "description": "The highest price of the stock on the specified date."
        },
        "low": {
          "type": "number",
          "description": "The lowest price of the stock on the specified date."
        },
        "close": {
          "type": "number",
          "description": "The closing price of the stock on the specified date."
        },
        "adjustedClose": {
          "type": "number",
          "description": "The adjusted closing price of the stock, reflecting factors like dividends and stock splits."
        },
        "volume": {
          "type": "number",
          "description": "The total number of shares traded on the specified date."
        }
      },
      "required": [
        "id",
        "stockId",
        "date",
        "open",
        "high",
        "low",
        "close",
        "adjustedClose",
        "volume"
      ]
    },
    "Signal": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Signal",
      "type": "object",
      "description": "Represents a generated trading signal for a stock, including its details and dispatch status via Telegram bot.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the Signal entity."
        },
        "stockId": {
          "type": "string",
          "description": "Reference to the Stock entity for which this signal was generated. (Relationship: Stock 1:N Signal)"
        },
        "generatedAt": {
          "type": "string",
          "description": "The date and time when the signal was generated locally by the app.",
          "format": "date-time"
        },
        "signalType": {
          "type": "string",
          "description": "The type of signal generated (e.g., 'BUY', 'SELL', 'HOLD', 'Strong Buy')."
        },
        "priceAtSignal": {
          "type": "number",
          "description": "The stock price at the moment the signal was generated."
        },
        "signalDescription": {
          "type": "string",
          "description": "A detailed textual description or rationale for the generated signal."
        },
        "rulesApplied": {
          "type": "string",
          "description": "Description of the predefined rules or parameters that were applied to generate this signal."
        },
        "status": {
          "type": "string",
          "description": "The current transmission status of the signal (e.g., 'PENDING', 'SENT', 'FAILED_RETRY', 'DELIVERED')."
        },
        "lastAttemptAt": {
          "type": "string",
          "description": "The date and time of the last attempt to dispatch this signal to the server/Telegram bot.",
          "format": "date-time"
        },
        "telegramMessageId": {
          "type": "string",
          "description": "The message ID returned by the Telegram API if the signal was successfully dispatched, useful for updates or deletions.",
          "format": "string"
        }
      },
      "required": [
        "id",
        "stockId",
        "generatedAt",
        "signalType",
        "priceAtSignal",
        "signalDescription",
        "rulesApplied",
        "status"
      ]
    }
  },
  "auth": {
    "providers": [
      "password",
      "anonymous"
    ]
  },
  "firestore": {
    "structure": [
      {
        "path": "/stocks/{stockId}",
        "definition": {
          "entityName": "Stock",
          "schema": {
            "$ref": "#/backend/entities/Stock"
          },
          "description": "Stores details about individual financial stocks. Documents in this collection are globally readable and serve as parent entities for historical price data. Includes denormalized data like 'lastHistoricalPriceFetchDate' for efficient status checks."
        }
      },
      {
        "path": "/stocks/{stockId}/historicalPrices/{historicalPriceId}",
        "definition": {
          "entityName": "HistoricalPrice",
          "schema": {
            "$ref": "#/backend/entities/HistoricalPrice"
          },
          "description": "Contains daily historical price and volume data for a specific stock. Each document is a subcollection of a 'Stock' document. This structure supports efficient querying of historical data for a given stock. The 'stockId' field in the document itself provides denormalized reference to the parent stock.",
          "params": [
            {
              "name": "stockId",
              "description": "The unique identifier of the parent Stock entity."
            },
            {
              "name": "historicalPriceId",
              "description": "The unique identifier for a specific historical price record, typically based on date."
            }
          ]
        }
      },
      {
        "path": "/signals/{signalId}",
        "definition": {
          "entityName": "Signal",
          "schema": {
            "$ref": "#/backend/entities/Signal"
          },
          "description": "Stores generated trading signals that are queued for dispatch via a Telegram bot. This collection is primarily managed by a backend service, with restricted write access for signal generation and read access for backend monitoring. Documents include 'stockId' for authorization independence, 'status' for explicit state modeling of dispatch, and 'telegramMessageId' for post-dispatch management.",
          "params": [
            {
              "name": "signalId",
              "description": "The unique identifier for a generated signal."
            }
          ]
        }
      }
    ],
    "reasoning": "The proposed Firestore structure for EMPORER prioritizes Authorization Independence and Structural Segregation to ensure clarity, security, and efficient querying."
  }
}
